import os

os.system('cls')

#print("Hello, World!")
#print("Eu de novo")
#print(1 + 1)
#x = 15
#print(x * 2)
#--------------------------------------------------------
# Tipos de dados
# numéricos, texto, booleano (lógico)
# estaticamente tipadas, ou seja, precisa declarar os tipos antes de usar
# Python é dinamicamente tipada, ou seja, você não precisa declarar, apenas colocar;
# Exemplo: x = 15 (já entendeu que 15 é do tipo inteiro)
# Uma linguagem fracamente tipada que nem o JavaScript, você combina tipos diferentes em operações e não dá problema.
# Python já é fortemente tipada, ele não deixa trabalhar com tipos diferentes, por exemplo:
# print(15 + "agua") = Erro (unsupported operand type(s) for +: 'int' and 'str')
